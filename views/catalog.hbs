{{> head}}
  <div class="page">
    <h2 class="page-title">Грунты</h2>

    <form class="filter" method="get" action="/soils">
      <label class="filter-label" for="maxLoosening">Коэффициент разрыхления не более</label>
      <div class="filter-row">
        <input class="input" type="text" id="maxLoosening" name="maxLoosening"
               value="{{maxLoosening}}" placeholder="1,50">
        <button class="btn btn-small" type="submit">Показать</button>
      </div>
    </form>

    <div class="grid">
      {{#each soils}}
        <a class="card" href="/soils/feed/{{this.id}}">
          <img class="card-image" src="{{../mediaUrl}}/{{this.imageName}}" alt="{{this.name}}">
          <div class="card-body">
            <div class="card-name">{{this.name}}</div>
            <div class="card-factor">K = {{num2 this.looseningFactor}}</div>
            <div class="card-likes">
              <svg class="heart" viewBox="0 0 24 24">
                <path d="M12 21c-.3 0-.6-.1-.8-.3C7.3 17.4 4 14.3 4 10.6 4 7.9 6 6 8.4 6c1.4 0 2.7.7 3.6 1.8C12.9 6.7 14.2 6 15.6 6 18 6 20 7.9 20 10.6c0 3.7-3.3 6.8-7.2 10.1-.2.2-.5.3-.8.3z"/>
              </svg>
              {{this.likes.length}}
            </div>
          </div>
        </a>
      {{/each}}
    </div>
  </div>
{{> tabbar}}
